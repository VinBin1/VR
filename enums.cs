using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class enums {

    public enum MethodEnum

    {
        ButtonClicked,
        Measure,
        place,
        blah,
        rofl,
        lmao

    }
    public enum Platform
    { Editor, Android, Go, other };
}
